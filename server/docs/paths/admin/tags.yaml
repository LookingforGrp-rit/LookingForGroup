post:
  summary: Create a tag.
  tags:
    - Admin
  description: Creates a tag with the given parameters.
  parameters:
    - in: query
      name: devId
      schema:
        type: integer
      required: true
      description: The userId being used for credentials.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../swagger.yaml#/components/schemas/TagInput'
  responses:
    '200':
      description: Tag created successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 200
              error:
                nullable: true
                example: null
              data:
                $ref: '../../swagger.yaml#/components/schemas/Tag'
    '400':
      $ref: '../../swagger.yaml#/components/responses/BadRequest'
    '500':
      $ref: '../../swagger.yaml#/components/responses/InternalServerError'
